Console.Write("Введите число N: "); 
    int num = Convert.ToInt32(Console.ReadLine());
    int i = 0;

    while (i <= num)
    {
        i++;
    int result = (i*i*i);
    Console.Write(result);
    }